1) Clone the Repository or download the zip file of code.
2) In the directory temperature-converter run the command (npm i) this will install all the required packages.
3) run command (npx expo start).
